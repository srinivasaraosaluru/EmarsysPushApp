import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lla',
  appName: 'EmarsysPushApplication',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
